import Image from "next/image";
import { ArrowUpRight, CalendarDays } from "lucide-react";
import type { CDEActivity } from "@/content/cdes/types";

export function ActivityCard({ activity }: { activity: CDEActivity }) {
  return <article className="group overflow-hidden rounded-[1.8rem] bg-paper shadow-card transition-transform hover:-translate-y-1"><div className="relative aspect-[1.35/1] overflow-hidden bg-secondary"><Image src={activity.image} alt={activity.title} fill className="object-cover transition duration-500 group-hover:scale-105" sizes="(max-width: 1024px) 90vw, 33vw" /><div className="absolute inset-0 bg-gradient-to-t from-ink/80 via-transparent to-transparent" /></div><div className="p-6"><div className="flex items-center gap-2 text-[10px] font-extrabold uppercase tracking-[0.12em] text-ink/45"><CalendarDays size={13} aria-hidden="true" />{activity.date}</div><h3 className="display-title mt-4 text-2xl leading-[0.98]">{activity.title}</h3><p className="mt-4 text-sm leading-6 text-ink/60">{activity.summary}</p>{activity.content && <details className="mt-5 border-t border-ink/10 pt-4"><summary className="focus-ring inline-flex cursor-pointer list-none items-center gap-2 text-sm font-extrabold text-secondary hover:text-primary">Leer actividad <ArrowUpRight size={16} aria-hidden="true" /></summary><p className="mt-4 text-sm leading-7 text-ink/65">{activity.content}</p></details>}{activity.href && <a href={activity.href} className="focus-ring mt-5 inline-flex items-center gap-2 text-sm font-extrabold text-secondary hover:text-primary">Ver detalle <ArrowUpRight size={16} aria-hidden="true" /></a>}</div></article>;
}
