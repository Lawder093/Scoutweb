import Image from "next/image";
import Link from "next/link";
import { ArrowUpRight, UsersRound } from "lucide-react";
import type { CDECommunity } from "@/content/cdes/types";

export function CommunityCard({ cdeSlug, community }: { cdeSlug: string; community: CDECommunity }) {
  return <Link href={`/cde/${cdeSlug}/comunidades/${community.id}`} aria-label={`Ver la comunidad ${community.name} del ${cdeSlug}`} className="group block min-w-0"><article className="overflow-hidden rounded-[1.7rem] border border-ink/10 bg-paper shadow-card transition-transform group-hover:-translate-y-1"><div className="relative flex h-40 items-end overflow-hidden bg-secondary p-6"><div className="absolute -right-7 -top-10 h-36 w-36 rounded-full border border-white/20" aria-hidden="true" />{community.image ? <Image src={community.image} alt={community.name} fill className="object-cover opacity-75" sizes="(max-width: 768px) 90vw, 30vw" /> : <UsersRound className="relative text-accent" size={42} strokeWidth={1.5} aria-hidden="true" />}<span className="relative rounded-full bg-accent px-3 py-1.5 text-[10px] font-black uppercase tracking-[0.12em] text-ink">{community.kind}</span></div><div className="p-6"><div className="flex flex-wrap items-start justify-between gap-3"><h3 className="break-words text-2xl font-extrabold">{community.name}</h3><span className="rounded-full bg-mist px-3 py-1 text-[10px] font-extrabold uppercase tracking-[0.1em] text-secondary">{community.ageRange}</span></div><p className="mt-4 text-sm leading-7 text-ink/60">{community.description}</p><span className="mt-5 inline-flex items-center gap-2 text-sm font-extrabold text-secondary transition-colors group-hover:text-primary">Ver comunidad <ArrowUpRight size={16} aria-hidden="true" /></span></div></article></Link>;
}
