import Image from "next/image";
import { Instagram, Linkedin } from "lucide-react";
import type { CDEEducator } from "@/content/cdes/types";

export function MemberCard({ educator }: { educator: CDEEducator }) {
  return <article className="group overflow-hidden rounded-[1.7rem] border border-ink/10 bg-paper shadow-card transition-transform hover:-translate-y-1"><div className="relative aspect-[1.1/1] overflow-hidden bg-mist"><Image src={educator.image} alt={`Foto de ${educator.name}`} fill className="object-cover grayscale transition duration-500 group-hover:scale-105 group-hover:grayscale-0" sizes="(max-width: 768px) 90vw, 30vw" /><div className="absolute inset-0 bg-gradient-to-t from-ink/65 via-transparent to-transparent" /><div className="absolute bottom-4 left-5"><p className="text-xs font-extrabold uppercase tracking-[0.13em] text-white/75">{educator.role}</p></div></div><div className="p-5"><h3 className="text-xl font-extrabold">{educator.name}</h3><span className="mt-2 inline-flex rounded-full bg-mist px-3 py-1.5 text-[10px] font-extrabold uppercase tracking-[0.1em] text-secondary">{educator.community}</span><p className="mt-3 text-sm leading-6 text-ink/60">{educator.bio}</p><div className="mt-5 flex flex-wrap gap-2">{educator.interests.map((interest) => <span key={interest} className="rounded-full bg-mist px-3 py-1.5 text-[10px] font-bold uppercase tracking-[0.1em] text-secondary">{interest}</span>)}</div><div className="mt-5 flex gap-2 border-t border-ink/10 pt-4"><a href="#educadores" className="focus-ring grid h-8 w-8 place-items-center rounded-full border border-ink/10 text-ink/50 transition-colors hover:border-primary hover:text-primary" aria-label={`Instagram de ${educator.name}`}><Instagram size={14} /></a><a href="#educadores" className="focus-ring grid h-8 w-8 place-items-center rounded-full border border-ink/10 text-ink/50 transition-colors hover:border-primary hover:text-primary" aria-label={`LinkedIn de ${educator.name}`}><Linkedin size={14} /></a></div></div></article>;
}
